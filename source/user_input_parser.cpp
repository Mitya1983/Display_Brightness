#include "user_input_parser.h"
#include <cstring>

bool MT::parse_user_input(const char *input)
{
    bool value_to_return = true;
    if (strcmp(input, "h") == 0 || strcmp(input, "help"))

    return value_to_return;
}
